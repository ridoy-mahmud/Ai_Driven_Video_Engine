# ══════════════════════════════════════════════════════════════════
#  Streamlit Community Cloud — Secrets Template
#  Copy this file to .streamlit/secrets.toml and fill in your values.
#  NEVER commit secrets.toml to git — it is already in .gitignore.
#
#  HOW TO USE:
#  1. Go to share.streamlit.io → your app → Settings → Secrets
#  2. Paste the contents of your filled-in secrets.toml there
#  3. The app auto-starts its own FastAPI backend in the background —
#     no separate server needed!
# ══════════════════════════════════════════════════════════════════

# ─── LLM Provider (REQUIRED — pick ONE) ────────────────────────
# Options: "openai" | "gemini" | "deepseek"
LLM_SOURCE = "openai"

# Generic key used by whichever provider is selected above:
LLM_API_KEY = "sk-xxxxxxxxxxxxxxxxxxxxxxxxxx"
LLM_BASE_URL = "https://api.openai.com/v1"
LLM_MODEL = "gpt-4o-mini"

# Provider-specific keys (only required for the provider you chose):
OPENAI_API_KEY   = "sk-xxxxxxxxxxxxxxxxxxxxxxxxxx"   # openai
GEMINI_API_KEY   = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" # gemini
GEMINI_MODEL     = "gemini-2.0-flash"
DEEPSEEK_API_KEY = "sk-xxxxxxxxxxxxxxxxxxxxxxxxxx"   # deepseek
DEEPSEEK_MODEL   = "deepseek-chat"

# ─── Default video script style ─────────────────────────────────
# Options: podcast|tech_talk|true_crime|motivation|science_explainer|debate|...
PROMPT_SOURCE = "tech_talk"

# ─── TTS Voice (edge TTS is FREE — no key needed) ───────────────
TTS_SOURCE  = "edge"
TTS_VOICE_1 = "en-US-AriaNeural"
TTS_VOICE_2 = "en-US-DavisNeural"
TTS_SPEED   = "1.1"

# ─── Stock video sources ─────────────────────────────────────────
# At least ONE is recommended. Both are free-tier keys.
MATERIAL_SOURCE = "pexels"        # pexels | pixabay | all
PEXELS_API_KEY  = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
PIXABAY_API_KEY = "xxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxx"

# ─── Optional: External API backend URL ─────────────────────────
# Leave blank to use the built-in embedded backend (default for Streamlit Cloud).
# Set this only if you deploy FastAPI separately on Railway/Render.
# API_BASE_URL = "https://your-fastapi-backend.railway.app"
